# 玩家美术与动画进度

更新时间：2026-02-21

## 本轮目标
- 将 `Player` 原图处理为可用 Sprite 资源。
- 从代码切图方案过渡到可视化 `Animator Controller` 方案。

## 已完成
1. 玩家美术裁剪（保留中心构图）
- 已将以下 4 张原图从 `1024x1024` 裁为 `960x960`：
  - `Assets/Game/Art/Sprite/Player/player.jpg`
  - `Assets/Game/Art/Sprite/Player/player_idle.jpg`
  - `Assets/Game/Art/Sprite/Player/player_jump.jpg`
  - `Assets/Game/Art/Sprite/Player/player_fall.jpg`
- 对应 `.meta` 的 Sprite rect 已同步为 `width: 960` / `height: 960`。

2. 生成处理后素材（用于动画构建）
- 已生成：
  - `Assets/Game/Art/Sprite/Player/Processed/player_cropped.png`
  - `Assets/Game/Art/Sprite/Player/Processed/player_idle_cropped.png`
  - `Assets/Game/Art/Sprite/Player/Processed/player_jump_cropped.png`
  - `Assets/Game/Art/Sprite/Player/Processed/player_fall_cropped.png`

3. 动画系统改造为可视化方案（代码已就绪）
- 新增 Animator 参数驱动脚本：
  - `Assets/Game/Scripts/Gameplay/PlayerAnimatorStateDriver.cs`
  - 参数：`IsRunning` / `IsMoving` / `IsJumping` / `IsFalling`
- 新增可视化动画构建器（自动创建 clip/controller，并绑定到 Player）：
  - `Assets/Game/Editor/PlayerVisualAnimationBuilder.cs`
  - 菜单：`Tools/Player/Build Visual Animator`
- 已移除 `RunnerController` 里的代码切图状态机逻辑，避免与 Animator 冲突：
  - `Assets/Game/Scripts/Gameplay/RunnerController.cs`

4. 编译状态
- 当前脚本编译通过（0 error，存在项目历史 warning）。

## 待执行（需要在已打开 Unity 编辑器内操作）
1. 点击菜单 `Tools/Player/Build Visual Animator`。
- 该步骤会自动：
  - 把 `Processed` 目录图片设置为 Sprite。
  - 生成 `AnimationClip` 与 `Player.controller`（可视化状态机）。
  - 给 `SampleScene` 的 `Player` 挂载 `Animator` 和 `PlayerAnimatorStateDriver`。
  - 保存场景改动。

## 说明
- 由于项目当时已被另一个 Unity 实例占用，命令行批处理写回场景被 Unity 锁拦截；因此改为在当前编辑器内一键执行最稳妥。
